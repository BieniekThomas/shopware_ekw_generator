const replaceVariables = (correctEkwName, otherInput, fileAsTxt) = {
	// do something
	return 	"a very long string";
}

module.exports = {
  replaceVariables: function (correctEkwName, otherInput, fileAsTxt) {
    outputString = replaceVariables(correctEkwName, otherInput, fileAsTxt);
    return outputString;
  }
};